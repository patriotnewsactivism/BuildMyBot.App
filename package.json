{
  "name": "buildmybot",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "npm run lint:web && npm run lint:functions",
    "lint:web": "tsc --noEmit",
    "lint:functions": "npm --prefix functions run lint",
    "build:functions": "npm --prefix functions run build",
    "test:rules:local": "vitest run tests/firestore.rules.test.ts",
    "test:functions:local": "vitest run tests/functions.proxy.test.ts",
    "test:rules": "firebase emulators:exec --project demo-buildmybot --only firestore \"npm run test:rules:local\"",
    "test:functions": "npm run build:functions && OPENAI_API_KEY=test-openai-key OPENAI_API_BASE=http://127.0.0.1:9393 firebase emulators:exec --project demo-buildmybot --only functions,firestore \"npm run test:functions:local\"",
    "test": "npm run test:rules && npm run test:functions"
  },
  "dependencies": {
    "@google/genai": "^1.31.0",
    "@supabase/supabase-js": "^2.39.7",
    "lucide-react": "^0.344.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "recharts": "^2.12.2"
  },
  "devDependencies": {
    "@types/node": "^20.19.26",
    "@types/react": "^18.2.64",
    "@types/react-dom": "^18.2.21",
    "@firebase/rules-unit-testing": "^3.0.1",
    "firebase": "^10.14.0",
    "firebase-tools": "^13.29.1",
    "@vitejs/plugin-react": "^4.2.1",
    "typescript": "^5.4.2",
    "vitest": "^1.6.0",
    "vite": "^5.1.5"
  }
}
