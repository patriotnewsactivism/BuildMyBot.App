// Database service - All operations now handled directly via Supabase client
// This file is kept for backwards compatibility but all logic has been migrated to app routes

export const dbService = {
  // All database operations now use Supabase client directly in components/pages
  // See app/dashboard/* for implementation examples
};
